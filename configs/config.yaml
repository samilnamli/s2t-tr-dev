# Root Hydra config.
#
# Usage:
#   python run.py experiment=ami_main_results
#   python run.py experiment=ami_main_results experiment.trainer.max_epochs=2
#   python run.py mlflow.tracking_uri=http://localhost:5000

defaults:
  - experiment: ami_main_results
  - _self_

mlflow:
  tracking_uri: mlruns
  experiment_name: ASR_Model_Selector

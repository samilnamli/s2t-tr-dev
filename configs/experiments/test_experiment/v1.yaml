experiment_metadata:
  name: test_experiment
  parent: null
  status: active
  created: 2026-05-02
  author: AI
  interpretation: "First test experiment to verify outputs organization."
  rationale: "We want to ensure all outputs are inside logs/ organized by experiment."
  hypothesis: "Logs will go to logs/test_experiment"

parquet_path: "data/processed/edinburghcstr_ami/combined_features_with_transcripts.parquet"
skip_rover: true
skip_training_free: true
shared:
  train_ratio: 0.8
  val_ratio: 0.1
  max_seq_len: 200
  batch_size: 16
  num_workers: 0
  max_epochs: 1
  learning_rate: 1e-4
  precision: "bf16-mixed"
  eager_load: false
  allow_tf32: false
  limit_batches: 2
  arch: "mlp_pool"

methods:
  - name: "baseline_hus_1"
    seed: [42]

  - name: "baseline_hus_2"
    seed: [42]